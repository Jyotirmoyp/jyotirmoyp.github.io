/* cv.js: behaviour used only on the CV page. */

/* Page banner: same mantle-convection engine as the other pages (see /js/mantle.js). */
MantleFlow(document.getElementById('mantle'), document.querySelector('.flow-toggle'));
