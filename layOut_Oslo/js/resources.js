/* resources.js: behaviour used only on the Resources page. */

/* Page banner: same mantle-convection engine as the home, Research, Outreach and Teaching pages
   (see /js/mantle.js). */
MantleFlow(document.getElementById('mantle'), document.querySelector('.flow-toggle'));
